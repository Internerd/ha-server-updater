{
  "config": {
    "step": {
      "user": {
        "title": "Add server",
        "description": "Connection details for the Linux server (Debian, Ubuntu or Proxmox VE). Provide either a password or a private key (or both, in which case the key is preferred).\n\n**Pasting a private SSH key**: On the machine that holds the key, print the private key file's contents (e.g. `cat ~/.ssh/id_ed25519` or `cat ~/.ssh/id_rsa`) and paste the **entire text** into the \"Private key\" field — including the first line `-----BEGIN ... PRIVATE KEY-----` and the last line `-----END ... PRIVATE KEY-----`. Do not use the `.pub` file, that is the public key. If the key is protected by a passphrase, enter it in the \"Passphrase\" field, otherwise leave it empty. The matching public key must already be present in the target server's `~/.ssh/authorized_keys` for the given user.",
        "data": {
          "name": "Name",
          "host": "Host / IP address",
          "port": "SSH port",
          "username": "Username",
          "password": "SSH password (optional if a key is used)",
          "private_key": "Private SSH key (optional if a password is used)",
          "private_key_passphrase": "Key passphrase (only if the key has one)",
          "use_sudo": "Run commands with sudo",
          "sudo_password": "Sudo password (leave empty for NOPASSWD or root login)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect: {error}",
      "auth_required": "Please provide either a password or a private key."
    },
    "abort": {
      "already_configured": "This server is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Server Updater options",
        "data": {
          "scan_interval_minutes": "Polling interval (minutes)",
          "refresh_apt_cache": "Refresh package list before each check (apt-get update)"
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "updates_available": {
        "name": "Updates available"
      },
      "reboot_required": {
        "name": "Reboot required"
      }
    },
    "button": {
      "update": {
        "name": "Update"
      },
      "update_and_reboot": {
        "name": "Update and reboot"
      }
    }
  }
}
